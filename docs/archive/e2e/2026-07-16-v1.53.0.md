# L3 端到端回归报告

> 模板发布前 L3 回归记录（归档留痕）。模板来源 `template-docs/e2e-report-template.md`；归档于 `docs/archive/e2e/`。

## 基本信息

- 待发布版本：v1.53.0（阶段 A，MINOR）
- 测试日期：2026-07-16
- 测试人：Claude（AI 维护者）+ 用户确认
- 环境：Windows 11 / Git Bash
- 测试派生项目：临时派生（e2e-sync-check.sh 造临时假派生，未用专用 e2e 仓）
- 关联：issue #221 阶段 A；分支 `feat/new-project-version-defaults`；commit `62aafb1`

## 自动化项（`bash scripts/e2e-sync-check.sh`）

- check-template（含同步链路 + doc-standards 镜像 + 新项目烟测 + 同步清单一致性）：✅ EXIT=0，1843 项全过
- sync-all-derived 批量烟测（造 2 假派生 + 1 非派生 dry-run）：✅ 通过
- 输出摘要：`✅ L3 可自动化回归通过（人工项见 checklist）`

## 人工项（按 `template-docs/e2e-regression-checklist.md`）

- R4 场景引导路由：**免实跑**。本改动不碰 `template-docs/scenario-guides.md`、`ai/commands/` 路由代码。
- R5 文档生成：**免实跑**。本改动不碰 `review-inputs` / `generate-docs` 文档生成 prompts。
- R6 PowerShell fallback：**免实跑**。本改动只改 `check-template.sh`，未碰任何 `.ps1`；PowerShell fallback 路径不受影响。

## 问题与归因

- 无。提交前 check-template 烟测 1 项失败（project-rules §2.8），根因 = `new-project.sh --local` 用 `git archive HEAD` 不含未提交改动（非缺陷）；提交后重跑全绿。

## 结论

- [x] 可发布
- [ ] 需修复后重测
- [ ] 阻塞发布

## 后续动作

- 阶段 B（存量迁移）/ 阶段 C（文档治理）独立 PR 时各跑各的 L3。
- C-001 字段命名（`Project version at sync time` → `at creation`）等待确认项见 `_proposals/TEMPLATE-UPGRADE-derived-version-mechanism.md` §4。
