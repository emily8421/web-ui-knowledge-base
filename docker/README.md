# docker/ 容器骨架

容器 / 外部服务骨架目录。**按项目形态裁剪**（决策记入 `ai/project-rules.md` §3）：

- **保留**：需要容器 / 外部服务（`docs/04-05` 写明运行拓扑）。
- **可不装 Docker**：纯本地烟测 / 纯脚本 / 纯前端。Demo 优先本机；资源不足再写服务器预案。

运行拓扑见 `docs/04-architecture.md`；资源约束见 `ai/project-rules.md` §2.1。
