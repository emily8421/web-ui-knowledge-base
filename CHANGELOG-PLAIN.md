# CHANGELOG-PLAIN

本文件记录派生项目自身版本历史的大白话说明；继承的母模板版本见 `TEMPLATE-BASE.md`。

## v0.1.1（2026-08-16）

把上次说了“不用”但一直没删的东西删掉了：四个空代码目录（frontend / backend / tests / docker，里面只有占位文件）和两份从没填过的数据库 / 接口设计骨架文档。项目规则里本来就声明了不用它们，这次让目录和声明对上。另外起草了一份给母模板的改进提案（让“按项目形态裁剪目录”这件事真正有人执行、有检查），准备回流。

## v0.1.0（2026-08-16）

初始化项目，基于 ai-project-template v1.62.0 创建。后续这里记录“这个项目自己每个版本改善了什么”，不记录母模板自身版本历史。
