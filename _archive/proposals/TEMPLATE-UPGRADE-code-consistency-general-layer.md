# TEMPLATE-UPGRADE：通用层代码一致性补充（#332 退回重写稿）

> 来源：LUMEN_demo_T2.1（emily8421/LUMEN-DEMO）派生项目回流；GitHub issue #332 退回重写稿
> 状态：已评审通过（2026-08-13），落地于 v1.61.4 PATCH
> 目标版本：v1.61.4（PATCH，已落地）
> Release impact：patch（落地于 v1.61.4）
> Release strategy：单独发布（独立评审，不并入其他批次）

## 1. 动机（去项目化）

原 #332 提案提出 3 条跨项目通用代码一致性补充（CI 质量门 / 关键 secret 启动校验 / 多实现显式契约）。复评（`docs/research/2026-08-12-c1-proposal-triage-reassessment.md` §3）判定：方向部分成立，但现稿「过强」——把固定实现（`test+type+lint` 三件套、`Protocol/ABC/interface`）固化为所有下游项目的硬规则，与项目形态多样性及既有裁剪口径冲突。处置为**退回重写**：从「固定实现」改为「原则 + 适用条件 + 项目化落地」。

本稿按该方向重写，并与 v1.61.3 新落的 `ai/implementation-lifecycle-rules.md §6.2`（自动检查归位与质量门口径）统一。3 条原则方向仍成立的实证（LUMEN 2026-08 全栈审查）：CI 只校验文档 / 版本 / 空白，后端约 30 个测试从未在 CI 跑且零 lint；关键 secret 弱默认值无启动校验也能启动；repository 双实现新增方法只加到一个实现时另一实现静默缺失。

## 1.1 与既有规则的关系（去重）

- **`global-rules §2.1` L0 通用代码原则**（`ai/global-rules.md`，现行）：管「写代码的下层基本功 + 可执行口径」。**层级不同**——L0 给原则，本稿给「适用条件 + 落地方式」；质量门口径不重抄 L0-8（指针链 L0-8 → §9.4 → §6.2）；多实现契约指针回指 L0-12，文本落在 §6.2，不触碰 L0 正文。
- **`implementation-lifecycle-rules §6 / §6.2`**（`ai/implementation-lifecycle-rules.md`，§6.2 于 v1.61.3 已落）：管「验证方案按项目形态裁剪 + 自动检查归位准则」。**合并入**——质量门口径已由 §6.2 承接，本稿在其末尾追加「应用说明」块（常见形态对照 + secret 底线 / 豁免 + 多实现契约），不重写主节与 §6.1。
- **`docs/05-tech-spec` 标准**（`template-docs/docs-scaffold/05-tech-spec.md`，现行）：管 secret 文档登记。**互补不重复**——本稿补「生产 / 安全敏感环境不得弱默认值 + 启动期 fail-closed 校验」执行底线与豁免规则，登记与豁免落点沿用既有文档。
- **`template-docs/web-fullstack-profile.md §9`**（#333 已落地）：管 Web 形态代码契约。**对象不同**——本稿是跨形态通用层，Web 专属写法在 profile，不重复。
- **#335（已关闭，选 A）**：**指向**——不引入 L0-L3 / R1-R7 编号体系；其「按 build 覆盖与适用性裁剪」立场已由 v1.61.3 §6.2 承接，本稿只引用不重复。
- **governance R1-R7 编号体系（未入库）**：**对象不同**——governance 框架与 consolidation map 不在模板仓，#335 选 A 已否决将其提升为模板正式概念，本稿不引入。
- **stack-adapters（R5，未建）**：**对象不同**——本稿不含栈具体写法（Settings 模块、Protocol 写法等），由未来 Stack Adapter 承载。
- **原 #332 三条建议（本稿前身）**：**指向**——本稿为 #332 退回重写稿，取代原稿「固定实现」表述，仅保留方向与实证。

**本提案不重复它们**：CI 必跑测试已由 L0-8 覆盖，质量门裁剪口径已由 §6.2 覆盖；本稿的独有价值是「secret 执行底线 + 多实现契约」的适用条件与落地说明，外加质量门「常见形态对照」示例。差异化：把 3 条原则统一挂到既有权威源（§6.2 / 指针回 L0-12）下，不新增规则层、不新增目录、不触碰 L0 正文。

## 2. 拟改（候选落地路径，待评审）

落点：`ai/implementation-lifecycle-rules.md §6.2` 末尾追加「应用说明」块（候选文本见 §2.4），三部分内容统一落此块——其中第 1 条（质量门对照）为 §6.2 已承接口径的常见形态示例、非新增规则，第 2/3 条（secret / 多实现契约）为新增补强说明；多实现契约以指针回指 `global-rules §2.1` L0-12，不在 L0 正文新增。最终落点由模板维护者定。

### 2.1 质量门（示例补充，非新增规则）

- **原则**：项目必须声明适用的自动质量门（test / type / lint / build 按形态选择），不得只校验文档 / 版本 / 空白。
- **定位**：§6.2 已完整承接该口径（含「#332 固定三件套退回重写后与本口径统一」），本稿不再新增规则，仅建议补「常见形态对照」示例，帮助派生项目落地声明。
- **适用条件**：不适用项必须显式说明理由（如无类型系统语言 → type 不适用；build 已覆盖 type check → 不重复跑；CLI 脚本无独立 test → 说明验证替代方式；纯文档 Sprint 豁免）。
- **声明位置**：`ai/project-rules.md §5` 或 `docs/05-tech-spec.md`；CI 实际落地与声明一致。

### 2.2 关键 secret 启动校验（保留方向，补适用条件）

- **原则**：关键 secret（token 签名密钥、数据库凭证、第三方 API key 等）不得使用弱默认值；生产或安全敏感运行态启动期校验，缺失或为默认值即启动失败（fail-closed）。
- **适用条件**：本地开发、测试、显式 Mock 场景允许受控替代配置；豁免时点与理由必须写入技术方案与验证计划（`docs/05-tech-spec.md` / `docs/09-verification.md`），不得静默带弱默认运行。
- **机制**：具体校验实现（Settings 模块、环境变量检查等）由技术栈决定，模板不预置写法。
- **落点**：§6.2 应用说明块第 2 条。

### 2.3 多实现契约（保留方向，去语言绑定）

- **原则**：同一接口的多个可替换实现（demo / 真实、mock / prod、read-replica / primary）必须共享机器可检查的契约或兼容性测试，禁止仅靠 docstring + 鸭子类型声明接口对齐——否则新增方法只加到一个实现时另一实现静默缺失。
- **适用条件**：机制由技术栈决定（Python `typing.Protocol` / ABC、Java interface、TS interface + 契约测试等），模板不写死；契约变更须显式。
- **落点**：文本落在 §6.2 应用说明块第 3 条，指针回指 L0-12；不在 L0 正文新增。

### 2.4 候选落地文本（§6.2 应用说明块草案）

> **应用说明（来源：#332 退回重写稿）**
>
> 1. **质量门常见形态对照**：按 §6 主节与本节目口径，常见形态示例——有类型系统项目 → type 检查纳入质量门；build 已覆盖类型检查 → 不重复单跑；CLI / 脚本项目无独立测试命令 → 以 smoke / 手动验证替代并在声明中说明理由；纯文档 / 纯配置项目 → 豁免。
> 2. **关键 secret 启动校验（fail-closed）**：关键 secret（token 签名密钥、数据库凭证、第三方 API key 等）在生产或安全敏感运行态不得使用弱默认值；启动期校验，缺失或为默认值即启动失败。本地开发、测试、显式 Mock 场景允许受控替代配置，豁免时点与理由写入 `docs/05-tech-spec.md` 与验证计划，不得静默带弱默认运行。具体校验实现由技术栈决定，模板不预置写法。
> 3. **多实现显式契约**：同一接口的多个可替换实现（demo / 真实、mock / prod、read-replica / primary）须共享机器可检查的契约或兼容性测试，禁止仅靠 docstring + 鸭子类型声明接口对齐；机制由技术栈决定（如 Python `typing.Protocol` / ABC、Java interface、TS interface + 契约测试）。为 `global-rules §2.1` L0-12「先契约后实现」在多实现一致性场景的应用说明。

## 3. 版本影响

- **本稿**：`none`（仅 `_proposals/` 草案，不改 VERSION）。
- **若落地**：PATCH 候选（说明性补强：`implementation-lifecycle-rules.md` 内补应用说明块，不新增同步目录 / 必填入口 / 强制 Gate / CI 断言 / 默认行为变化），判据 `CONTRIBUTING §4`；具体版本号在实施 PR 时定。
- 若评审认为「常见形态对照表」构成新的推荐工作流或必填声明，则需重新论证 MINOR——本稿默认不引入。

## 4. 影响面

- **候选改动文件**：`ai/implementation-lifecycle-rules.md`（§6.2 末尾应用说明块；在 `template-sync.json files_all`，会下行同步）。
- **不触碰**：L0 §2.1 条目本身与正文、§6 主节开门句与测试等级矩阵、§6.1 DB guard、`template-docs/web-fullstack-profile.md`、`check-template` 断言、`template-sync.json` 结构、VERSION 机制。
- **check-template 影响**：零失败风险（纯说明性追加，不动被断言字符串）；不新增断言。
- **预期效果**：派生项目质量门声明、secret 底线与多实现契约有统一适用口径，与 §6.2 共同构成完整通用层说明。

## 5. 验证方式

- `scripts/check-template.ps1` 全绿（若实施落地）。
- 本稿 §1.1 自身作为去重声明样本，关系标签齐全且单一（dogfood）。
- §6.2 指针（`web-fullstack-profile §9.4` → §6）未断；应用说明块与 §6.2 现有 3 条、blockquote 口径措辞一致。
- 抽查三处措辞（质量门 / secret / 多实现契约）与 §6.2、L0-12 不重复定义；L0 正文零改动。

## 6. 后续

- 本稿为 #332 重写稿 v2：评审通过后，维护者决定是否更新 issue #332 正文 / 评论、是否进入落地分支 → check-template → PR（需用户授权）。
- 未通过则继续留在 `_proposals/` 候选池，不关闭 #332。
