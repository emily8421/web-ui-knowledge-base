# Token Hotspot 汇总：2026-08-16 ～ 2026-09-19

> 本文件为入库的阶段性汇总（提炼自本地 `.ai/token-hotspots/` 单条记录）；单条原始记录仍留本地（gitignored）。派生项目自建汇总，非母模板同步件。

## 0. 覆盖边界

- 已覆盖记录（本地 `.ai/token-hotspots/`）：
  - `2026-08-16-derived-dir-trim-analysis.md`（目录归属分析 + 裁剪 + 提案起草）
  - `2026-08-26-phase1-docs-preflight.md`（Phase1 docs 预检）
  - `2026-08-31-collect-source-build.md`（collect-source 技能建设 Sprint-KB1）
  - `2026-09-19-sync-template-v1.75.3.md`（模板同步 v1.75.3 闭环）
- 未覆盖记录：无（`.ai/pitfalls/` 下 2 份为坑观察日志，不属于 hotspot 汇总范围）
- 下一次 rollup 起点：从 2026-09-19 之后起，只统计「汇总状态：未汇总」的本地记录

## 1. 汇总范围

| 日期 | 任务类型 | 主要热点 |
|---|---|---|
| 2026-08-16 | 模板方法论分析 / 治理 | 完整规则路由读取；`template-sync.json` / `new-project.sh` / `capability-packages.md` 大文件；引用核查 grep 未分层 |
| 2026-08-26 | 同步后审计 + 输入评审预检 | 连续读取规则 / 标准 / 知识库材料；假设路径混入预检批次 |
| 2026-08-31 | 编码 + 文档 + 首跑实测 | `session-rules.md` 全文（收尾只需 §4–§6）；SOP / sources 规划与执行重复读；大站 HTML/CSS 报告截断控制 |
| 2026-09-19 | 模板维护 / 同步（完整回退包强制路径） | 回退包 8 文件 + 12/15 号 Prompt 全文；upstream CHANGELOG 跨度段；checkpoint + grep 摘要控制住长输出 |

## 2. 为什么触发 / 为什么此前未触发

首次 rollup。3 份记录跨 2026-08-16 → 08-31 逐步累积但各自独立成段，未达提示时机；2026-09-19 同步任务（命中 §4.1「模板维护 / 同步整理较长任务」触发条件）产生第 4 份，累计 ≥3 触发 §4.2 rollup。

## 3. 重复热点模式

1. **规则整包读取**：治理 / 模板维护 / 同步类任务按路由表要求读完整回退包（8 文件），是最大固定成本；属规则强制而非冗余，但非治理类任务存在「全文可裁章节」空间（如 `session-rules.md` 收尾场景只需 §4–§6）。
2. **大文件 / 长输出**：模板清单、脚本全文、场景手册、大站 HTML/CSS 抽取报告；已有有效对策——`head -c` 截断、`--format json` 按键提取、重定向日志 + grep 摘要。
3. **引用核查未分层**：grep 引用时不分「同步件 / 项目件」，同步件通用描述命中占大头且不可操作。
4. **重复读取 / 假设路径**：同一材料在规划与执行阶段各读一遍；预检批次混入未核实存在的假设路径。

## 4. 已形成的改进建议

- **必须保留**：治理 / 同步类任务的完整回退包读取（路由表强制，保证不跑飞）；长输出截断 / json 提取 / 日志 grep 三板斧。
- **应压缩**：引用核查先按 `template-sync.json` 分层过滤再 grep；收尾类请求按章节精读（`session-rules.md` §4–§6）。
- **应沉淀**：v1.73.0 章节级路由标注已随 v1.75.3 同步生效——后续回流 / 评估 / 回测类任务按 `ai/index.md` 标注章节读取即合规，本条模式预计自然消退。
- **应拆会话**：暂无（各任务均为单链闭环，无跨任务上下文污染）。

## 5. 模板回流判断

无需新增回流提案：最大痛点「规则读取成本」已由上游 v1.73.0（#441 章节级路由标注）覆盖并在本仓生效；「session-rules 收尾只读 §4–§6」属细粒度裁读观察，留待 §4.2 后续 rollup 复核是否普遍化，不单独立案。
