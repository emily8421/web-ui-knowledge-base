# backend/ 后端代码骨架

后端代码骨架目录。**按项目形态裁剪**（决策记入 `ai/project-rules.md` §3）：

- **保留**：有持久化存储 / 对外接口（配合 `docs/06` 数据库设计、`docs/07` 接口）。
- **可删**：纯前端 / 纯计算 / 无后端的项目。

裁剪规则见 `docs/README.md`；项目形态判断见 `ai/project-rules.md` §3。
