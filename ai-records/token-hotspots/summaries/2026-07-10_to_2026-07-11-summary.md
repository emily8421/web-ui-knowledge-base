# Token Hotspot Summary

- Period: 2026-07-10 to 2026-07-11
- Records included:
  - `2026-07-10-token-hotspot-mechanism-proposal.md` (record #1)
  - `2026-07-10-windows-smoke-test-guidance.md` (record #2)
  - `2026-07-11-domain-template-lab-pr-eval.md` (record #3)
- Projects / task types: ai-project-template; proposal-evaluation / template-maintenance / smoke-test / PR-closure / resume.
- Top repeated hotspots:
  - Full rule load from `ai/index.md` after a quick-resume escalated into task execution — appeared in all 3 records.
  - `scripts/check-template.sh` full success logs re-entered working context during PR validation (records #2, #3).
  - `git status --short --branch` / `git log` repeated after each PR, amend, push, merge, pull, and cleanup.
  - `.ai/session-handoff.md` re-read and patched multiple times per task to keep metadata aligned.
- Low-value high-cost reads:
  - Full `check-template.sh` success output after the exit code is already known — low value, large log (records #2, #3).
  - Full-file reads triggered only because earlier command output was truncated; targeted `Select-String` / line ranges would have sufficed once the target section was known (record #2).
  - Re-reading PR files to verify another AI's evaluation claims was partly redundant (record #3).
- High-value high-cost reads:
  - Full `ai/index.md` rules before any write / proposal / version / PR / merge governance step (all records).
  - `template-sync.json` + `scripts/sync-template.sh` + `template-docs/domain-templates.md` full reads to judge main-path pollution (record #3).
  - `template-docs/smoke-test.md` + `check-prereqs.ps1` + `new-project.sh` snippets to find the three user-facing follow-up fixes (record #2).
  - Failed CI log for PR #169 — quickly isolated a single trailing-blank-line defect (record #2).
- Reads that must stay:
  - Full rules before the first analysis / writing / merge step in a session (governance gate; not optimizable away).
  - Main-path file reads before any "safe to merge" verdict.
  - Local `check-template.sh` before push (de-risks CI).
  - `.ai/session-handoff.md` updates at each continuation point.
- Proposed template changes:
  - **Candidate rule (not yet landed): same-session rule-reuse.** Once full rules are loaded for the first analysis / writing task in a session, sequential governance steps (edit / amend / push / merge / handoff) may reuse already-loaded rules when no relevant rule file changed. Observed as avoidable re-read in all 3 records.
  - **Candidate convention: multi-AI claim→evidence table.** When cross-checking another AI's conclusion, request a claim→evidence (file:line) table so the verifier spot-checks specific lines instead of re-reading full files (record #3).
  - **Convention: validation-evidence summaries.** Record command name + exit code for successful long checks; avoid pasting full `check-template.sh` success logs into working context (records #2, #3).
  - Prefer `gh pr view --json statusCheckRollup` for routine checks; `gh run view --log-failed` only on failure.
- Risk if optimized:
  - Skipping rule load before writes / PR / merge, skipping main-path file reads before a merge verdict, or skipping local `check-template.sh` before push would weaken governance and release safety.
  - The same-session reuse rule must be scoped to "no relevant rule file changed"; otherwise stale-rule edits could slip in.
- Recommendation: small-doc-clarification — register the same-session rule-reuse candidate and the claim→evidence convention in the proposal for evaluation; do not yet promote to formal synced rules. Re-evaluate after 2–3 more records whether it warrants a formal rule in `ai/session-rules.md`.

## Notes

- First summary for the candidate mechanism in `_proposals/TEMPLATE-UPGRADE-token-hotspot-records.md`; meets the 3-record threshold in proposal §7.
- Created in the template repository; the proposed changes are distilled into `_proposals/` (this summary feeds back into the proposal) rather than a derived-project feedback issue.
- All 3 records are template-repo maintenance tasks (proposal eval / smoke-test / PR closure); samples skew toward maintainer scenarios. Per proposal §7.2, judging ordinary derived-project cost still needs 1–2 active derived-project trial records.
