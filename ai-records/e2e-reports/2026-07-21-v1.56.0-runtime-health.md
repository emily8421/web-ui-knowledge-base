# L3 端到端回归报告 — v1.56.0

> 模板发布前 L3 回归记录，按 `template-docs/e2e-report-template.md` 填写。
> 位置说明：模板指引可选 `docs/archive/e2e/` 或本地；本次放 `ai-records/e2e-reports/`（维护者侧，不下行同步、不被 `new-project.sh` 复制，避免污染派生 docs 骨架）。

## 基本信息

- 待发布版本：v1.56.0（运行时版本健康检测阶段 2，MINOR）
- 测试日期：2026-07-21
- 测试人：mmemily + Claude
- 环境：Windows 11 / Git Bash 2.54.0 / PowerShell 5.1.26100 / gh 2.93.0
- 测试派生项目：未建专用 `ai-project-template-e2e`（R1/R2/R3 由 `e2e-sync-check.sh` 自建临时派生覆盖；R4/R5/R6 豁免，见下）

## 自动化项（`bash scripts/e2e-sync-check.sh`）

- check-template（含同步链路 + doc-standards 镜像 + 新项目烟测）：✅
- sync-all-derived 批量烟测：✅
- 输出摘要：
  - `==> L3 端到端回归发布门` → `[1/2] check-template 通过` → `[2/2] sync-all-derived 批量 dry-run 通过` → `✅ L3 可自动化回归通过`，EXIT 0，无 fail markers。
  - 同步链路验证了 `scripts/check-runtime.ps1` 经 `template-sync.json` 新增一行下行同步不破坏同步判断。

## 人工项（按 `template-docs/e2e-regression-checklist.md`）

- R4 场景引导路由：**豁免** — 本次改动不触及 `template-docs/scenario-guides.md` / scenario 路由逻辑（零代码改动）。
- R5 文档生成（review-inputs + generate-docs → 00-09）：**豁免** — 本次改动不触及文档生成 prompts / `ai/doc-standards/`（零代码改动）。
- R6 PowerShell fallback：**豁免** — 本次改动未改 `check-template.ps1` / `sync-template.ps1` / `check-derived-sync.ps1` 的 fallback 逻辑；`check-template.ps1` fallback 按提案 §4 明确不镜像 check-runtime 新断言。

**豁免裁决理由**：R4/R5/R6 覆盖的三项能力（scenario 路由、文档生成、PS fallback）本次改动均零触及；本次改动对端到端能力的唯一相关风险是「新增同步件 `check-runtime.ps1` 的下行同步链路」，已由 R1/R2/R3 自动验证覆盖并全过。维护者确认接受该豁免口径。

## 问题与归因

- 无阻断问题。
- 附带发现（非阻断，已验证为预期行为）：本机（维护者机器）实跑 `check-runtime.ps1` 命中 issue #238 真病灶 —— node 解析到 `C:\Users\maixh\AppData\Local\Volta\tools\image\node\16.13.0\node.exe` 绕过 shim、`Volta + nvm-windows` 共存、PATH scope 为 session-only。这正是阶段 2 要交付的诊断能力的真实样本，证明 check-prereqs（响亮 warning）+ check-runtime（根因定位）配合有效。

## 结论

- [x] 可发布
- [ ] 需修复后重测
- [ ] 阻塞发布

## 后续动作

- 下一个触及 scenario 路由 / 文档生成 / PowerShell fallback 的 MINOR / MAJOR 发布前，补建专用 `ai-project-template-e2e` 派生项目，完整跑 R4/R5/R6 并建立基线报告（目前无历史 e2e 报告，R4/R5/R6 基线缺失）。
- 本次改动相关的 L3 风险（同步链路）已由 R1/R2/R3 全过覆盖，可进 PR。
