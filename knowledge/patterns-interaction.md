# 交互 Pattern 记录（可复用交互解法）

> 模型：Pattern（交互）；字段口径同母模板 `template-docs/ui-knowledge/interaction-patterns.md`。母模板核心层条目不在本文件重复定义；本仓扩展 ID 从 `PAT-INT-101` 起。

## 0. 元信息

| 项 | 内容 |
|---|---|
| 记录类型 | Pattern（交互），本仓扩展层 |
| ID 段位 | `PAT-INT-101+`（001–099 保留给母模板核心层） |
| 数量 | 0（首批待收集） |
| 状态说明 | 交互模式证据要求高于视觉（需行为契约 / 键盘焦点 / 状态恢复），首批从 A/B 级来源（APG / GOV.UK）抽取，不从 D 级案例拔高 |

## 1. 记录清单

（暂无。候选方向：主题切换的用户控制与持久化（可访问性偏好联动 `prefers-color-scheme`）——待来源核验后成文。）

---

字段模板（新记录复制此块）：

```markdown
### PAT-INT-1NN｜<模式名>

- **类型与维度**：Pattern / <维度>
- **要解决的问题**：
- **建议**：
- **适用条件**：
- **不适用条件**：
- **行为契约**：（触发 / 状态变化 / 反馈 / 恢复 / 键盘焦点——交互模式必填）
- **视觉信号**：
- **来源**：`SRC-*`
- **证据等级**：A/B/C/D
- **状态**：candidate / reviewed（日期）
```
